{
  "name": "liquidvm",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Simulation de fluides 2D temps réel en WebGPU (Stable Fluids, multi-fluides)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.64",
    "typescript": "~5.9.0",
    "vite": "^7.0.0"
  }
}
